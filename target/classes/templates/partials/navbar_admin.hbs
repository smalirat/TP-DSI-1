<nav class="navbar navbar-expand-lg navbar-light" style="background: #fff; border-bottom: 1px solid #e5e5e5;">
    <div class="container-fluid">
        <a class="navbar-brand fw-bold" href="/admin" style="color: #333;">Administración - MetaMapa</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <a class="nav-link {{#if (eq activeTab 'solicitudes')}}active{{/if}}" 
                       {{#if (eq activeTab 'solicitudes')}}aria-current="page" style="font-weight: bold; color: #007bff;"{{/if}} 
                       href="/admin/solicitudes">Solicitudes</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link {{#if (eq activeTab 'colecciones')}}active{{/if}}" 
                       {{#if (eq activeTab 'colecciones')}}aria-current="page" style="font-weight: bold; color: #007bff;"{{/if}} 
                       href="/admin/colecciones">Colecciones</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link {{#if (eq activeTab 'fuentes')}}active{{/if}}" 
                       {{#if (eq activeTab 'fuentes')}}aria-current="page" style="font-weight: bold; color: #007bff;"{{/if}} 
                       href="/fuentes">Fuentes</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link {{#if (eq activeTab 'hechos')}}active{{/if}}" 
                       {{#if (eq activeTab 'hechos')}}aria-current="page" style="font-weight: bold; color: #007bff;"{{/if}} 
                       href="/admin/hechos">Hechos</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link {{#if (eq activeTab 'estadisticas')}}active{{/if}}" 
                       {{#if (eq activeTab 'estadisticas')}}aria-current="page" style="font-weight: bold; color: #007bff;"{{/if}} 
                       href="/admin/estadisticas">Estadísticas</a>
                </li>
            </ul>
            <ul class="navbar-nav mb-2 mb-lg-0">
                {{#if usuarioUsername}}
                    <li class="nav-item">
                        <span class="nav-link">Sesión: <strong>{{usuarioUsername}}</strong> 
                            <span class="badge bg-secondary ms-2">{{usuarioRol}}</span>
                        </span>
                    </li>
                    <li class="nav-item">
                        <form method="post" action="/logout" style="display:inline">
                            <button type="submit" class="btn btn-danger ms-2">Cerrar Sesión</button>
                        </form>
                    </li>
                {{else}}
                    <li class="nav-item">
                        <a href="/login" class="btn btn-outline-primary ms-2">Iniciar Sesión</a>
                    </li>
                {{/if}}
            </ul>
        </div>
    </div>
</nav>