<nav class="navbar navbar-expand-lg navbar-light" style="background: #fff; border-bottom: 1px solid #e5e5e5;">
    <div class="container-fluid">
        <a class="navbar-brand fw-bold" href="/" style="color: #333;">MetaMapa</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <a class="nav-link {{#if (eq activeTab 'home')}}active{{/if}}" 
                       {{#if (eq activeTab 'home')}}aria-current="page" style="font-weight: bold; color: #007bff;"{{/if}} 
                       href="/">Inicio</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link {{#if (eq activeTab 'mapa')}}active{{/if}}" 
                       {{#if (eq activeTab 'mapa')}}aria-current="page" style="font-weight: bold; color: #007bff;"{{/if}} 
                       href="/mapa">Mapa Interactivo</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link {{#if (eq activeTab 'colecciones')}}active{{/if}}" 
                       {{#if (eq activeTab 'colecciones')}}aria-current="page" style="font-weight: bold; color: #007bff;"{{/if}} 
                       href="/colecciones">Colecciones</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link {{#if (eq activeTab 'hechos')}}active{{/if}}" 
                       {{#if (eq activeTab 'hechos')}}aria-current="page" style="font-weight: bold; color: #007bff;"{{/if}} 
                       href="/hechos">Hechos</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link {{#if (eq activeTab 'estadisticas')}}active{{/if}}" 
                       {{#if (eq activeTab 'estadisticas')}}aria-current="page" style="font-weight: bold; color: #007bff;"{{/if}} 
                       href="/estadisticas">Estadísticas</a>
                </li>
            </ul>
            <ul class="navbar-nav mb-2 mb-lg-0">
                {{#if usuarioUsername}}
                    <li class="nav-item">
                        <span class="nav-link">Sesión: <strong>{{usuarioUsername}}</strong> 
                            <span class="badge bg-secondary ms-2">{{usuarioRol}}</span>
                        </span>
                    </li>
                    {{#if (eq usuarioRol 'ADMINISTRADOR')}}
                        <li class="nav-item">
                            <a href="/admin" class="btn btn-outline-secondary ms-2">Panel Admin</a>
                        </li>
                    {{/if}}
                    <li class="nav-item">
                        <form method="post" action="/logout" style="display:inline">
                            <button type="submit" class="btn btn-danger ms-2">Cerrar Sesión</button>
                        </form>
                    </li>
                {{else}}
                    <li class="nav-item">
                        <a href="/login" class="btn btn-outline-primary ms-2">Iniciar Sesión</a>
                    </li>
                    <li class="nav-item">
                        <a href="/registro" class="btn btn-primary ms-2">Registrarse</a>
                    </li>
                {{/if}}
            </ul>
        </div>
    </div>
</nav>